# Georgia Hills Admin v3

Single-owner admin portal built with React + Vite.

## Commands

- `npm run dev`
- `npm run build`
- `npm run preview`

This app is intended to run in parallel with legacy `admin-v2` during migration.
